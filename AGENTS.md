# Медкарта: project contract

Preserve global Obol policy. Keep medical records out of Git, shared knowledge, issue/task bodies, telemetry and logs. Use synthetic fixtures only. This pilot currently contains documentation, not a deployed health service.

MapleGPT owns iOS HealthKit access; medkarta owns ingestion, provenance, storage and views. Coordinate through existing MapleGPT task 471; do not overwrite its active dirty checkout. No implicit transmission to model providers. All medical-data destination, scope and user consent must be explicit in the app before a real transfer.

Preserve source records, units, time zones, import provenance and corrections. Missing is not zero. Never invent diagnoses from measurements. Derived OCR/AI fields need source links and review status.

All actionable web notices must offer safe Скопировать для LLM diagnostics; omit actual health values, names and documents by default. Real data rollout requires verified private access, encryption, restore and deletion behavior on an approved worker. No controller background processes.
